<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idus.assignment.domain.dao.MemberDao">

    <select id="selectOneById" parameterType="String" resultType="com.idus.assignment.domain.dto.Member">
        select * from member where id = #{id}
    </select>

</mapper>